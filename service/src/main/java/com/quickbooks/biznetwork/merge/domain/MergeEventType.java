package com.quickbooks.biznetwork.merge.domain;

public enum MergeEventType {
    MERGE_PROPOSED,
    MERGE_CONFIRMED,
    CONSOLIDATION_STARTED,
    CONSOLIDATION_COMPLETED,
    MERGE_REVERSE_REQUESTED,
    MERGE_REVERSED
}
